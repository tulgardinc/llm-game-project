[
  {
    "name": "getAllCharacterNames",
    "description": "Returns all known character names that are in the database.",
    "function": "getAllCharacterNames"
  },
  {
    "name": "getCharacter",
    "description": "Returns the details of a character from the database given their name.",
    "function": "getCharacter",
    "parameters": {
      "type": "object",
      "properties": {
        "charName": {
          "type": "string",
          "description": "The name of the character you are querying"
        }
      },
      "required": ["charName"]
    }
  },
  {
    "name": "addCharacter",
    "description": "Adds a new character with a name and a description to the database, returns the added character if successfull.",
    "function": "addCharacter",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the character"
        },
        "description": {
          "type": "string",
          "description": "Description of the character"
        }
      },
      "required": ["name", "description"]
    }
  }
]
